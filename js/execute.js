import { EngineState } from "./index.js";
import { Stack } from "./stack.js";

/**
 * 
 * @param {string} code 
 * @param {EngineState} engineState 
 * @param {Stack} stack 
 */
export function execute(code, engineState, stack) {
  
}